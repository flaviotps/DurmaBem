package com.flaviotps.durmabem

object ConfigConstants {
     object SoundPool{
            const val DEFAULT_MAX_POOL = 10
    }

    object Sound {
        const val DEFAULT_SPEED = 1f
        const val DEFAULT_VOLUME = 1f
        const val DEFAULT_LOOP = true
    }
}